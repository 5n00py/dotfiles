# tmux-cpu is a plugin that displays CPU usage and load in the status bar. 
# It allows for real-time monitoring of system resources directly within the 
# tmux session.
set -g @plugin 'tmux-plugins/tmux-cpu'


